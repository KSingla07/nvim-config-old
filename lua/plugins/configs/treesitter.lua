local options = {
  ensure_installed = { "lua", "go", "sql", "dockerfile", "make" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
